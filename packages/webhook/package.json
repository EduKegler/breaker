{
  "name": "@trading/webhook",
  "version": "1.0.0",
  "description": "TradingView webhook receiver — validates alerts, deduplicates, and forwards to WhatsApp Gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "start": "node dist/webhook/server.js"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "@types/supertest": "^6.0.3",
    "supertest": "^7.2.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "express": "^4.21.0",
    "express-rate-limit": "^8.2.1",
    "ioredis": "^5.9.3",
    "lru-cache": "^11.2.6",
    "zod": "^3.24.0"
  }
}
