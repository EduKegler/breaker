{
  "name": "@trading/whatsapp-gateway",
  "version": "0.1.0",
  "description": "WhatsApp messaging gateway — send and receive messages via Evolution API",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "start": "node --env-file=.env dist/server.js"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "@types/supertest": "^6.0.3",
    "supertest": "^7.2.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "express": "^4.21.0",
    "zod": "^3.24.0"
  }
}
